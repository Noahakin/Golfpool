services:
  - type: web
    name: golfpool-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SCRAPERAPI_KEY
        sync: false
